extends base.html
block content
  block content_title
  for article in articles_page.object_list
    section
      header.major
        h2= article.title
      p.time #{article.locale_date}
      br
      br
      != article.summary
      ul.actions
        li
          a.button(href="#{SITEURL}/#{article.url}") Read More
